output {
  if [type] == "beats_log" {
    elasticsearch {
      hosts    => "{{ elastic_hosts }}"
      index    => "chat-friendsoft-%{+YYYY.MM.dd}"
    }
  }
  
  if [type] == "http_rest" {
    elasticsearch {
      hosts    => "{{ elastic_hosts }}"
      index    => "http-rest-%{+YYYY.MM.dd}"
    }
  }

  if [type] == "exchange" {
    elasticsearch {
      hosts    => "{{ elastic_hosts }}"
      index    => "exchange-log-%{+YYYY.MM.dd}"
    }
  }
}
